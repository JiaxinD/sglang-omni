# HANDOFF — PR #745 (feat/moss-local-state-pool)

Session on `sgl-project/sglang-omni` PR #745. PR head was `7ee050b`; I pushed
`2f33b06` (category-a) to the live PR, and staged `2024b80` (Phase 2) on the
fork branch `origin/claude/friendly-keller-tv8t5e` pending your go.

> **Reality vs the brief:** most of the brief was already done on the branch
> before this session. Phase 1 (`reset_for_refill` pool API + retraction wiring
> + both tests) landed in `7ee050b`; the branch is **not** behind main
> (`merge-base(head, main) == main`), so the Phase-1 merge step was unneeded.
> Hayden's `525cc17` (lifecycle hardening) and the merge `f7df11b` are in.
> Remaining real work was the Ratish review wave + Phase 2.

---

## 1. Triage table (every #745 review thread)

Legend: ✅ done this session · ☑️ already fixed pre-session · ⏳ open, queued ·
🔎 investigated (no code) · ✍️ draft only (held).

| # | Thread (anchor) | Reviewer | Topic | Class | Status |
|---|---|---|---|---|---|
| 1 | r3393172282 | code-quality | `_AUDIO_END_ID` unused | a | ☑️ resolved pre-session |
| 2 | r3394136367 | Ccyest | encapsulate `_params_written_rids` | a | ☑️ done (`ensure_/invalidate_params`, 7ee050b) |
| 3 | r3394326869 | edwingao28 | row-leak `finally` in result adapter | bug | ☑️ Hayden 525cc17 |
| 4 | r3394326887 | edwingao28 | journal `IndexError` tripwire | bug | ☑️ resolved |
| 5 | r3394326906 | edwingao28 | c2 guard scatter on chunked | bug | ☑️ resolved |
| 6 | r3394326915 | edwingao28 | nit: double H2D upload | nit | ☑️ resolved |
| 7 | r3395172184 | gaoyang07 | `reset_for_refill` API | a | ☑️ done 7ee050b → **you resolve** (draft B) |
| 8 | r3395352785 | gaoyang07 | **GPU-native radix hash** | Phase2 | ✅ impl `2024b80` (staged) · ✍️ 11b · no-resolve (GPU pending) |
| 9 | r3398040368 | Ratish1 | trim abort_request comment | a | ✅ 2f33b06, replied+resolved |
| 10 | r3398044077 | Ratish1 | trim bf16 comment | a | ✅ 2f33b06, replied+resolved |
| 11 | r3398048066 | Ratish1 | remove test banner | a | ✅ 2f33b06, replied+resolved |
| 12 | r3398085976 | Ratish1 | remove gate script | c | 🔎 D2 · ✍️ c2 · hold |
| 13 | r3398199811 | AkazaAkane | vectorize into [P] tensors | c | ✍️ 11c · hold (follow-up) |
| 14 | r3398238552 | Ratish1 | remove parity harness file | c | 🔎 D2 · ✍️ c2 · hold |
| 15 | r3398358018 | Ratish1 | **chunked `generation_steps`** | c1 | 🔎 D1 (investigated, recommend B1) · hold |
| 16 | r3398507661 | Ratish1 | PR-B codename in docstring | a | ✅ 2f33b06, replied+resolved |
| 17 | r3398513076 | Ratish1 | rename `…Journal`? | c | 🔎 D3 · ✍️ c3 · hold |
| 18 | r3398517657 | Ratish1 | trim module docstring | a | ✅ 2f33b06, replied+resolved |

Issue comments: Ccyest "fix lint" → lint is **green** now (done). edwingao28
"left some reviews" → informational.

---

## 2. Commits

| SHA | Where | Summary |
|---|---|---|
| `2f33b06` | **on live PR** | docs: trim review-flagged comments, drop PR-A/PR-B codename (category-a, threads 9/10/11/16/18) |
| `2024b80` | **fork only (staged)** | perf: capture-safe GPU radix hash for generated rows (Phase 2, thread 8) |

Patches in `handoff/patches/`. To land Phase 2 on the live PR:
`git push upstream 2024b80:feat/moss-local-state-pool` (access confirmed).

---

## 3. Test results

- **`tests/unit_test/moss_tts_local/test_radix_hash.py` — 7/7 PASS** (CPU,
  torch 2.5.1). Covers determinism, zero-collision (adjacent/permuted), EOS
  preservation, output domain, dtype/device, int32 input, and an exact
  bignum-reference cross-check (no int64 overflow).
- category-a change is comment/docstring-only; `py_compile` clean.
- **Could NOT run here:** `test_state_pool.py` / `test_pipeline.py` and the S0
  gate — they need `sglang` + `msgpack` + the runtime stack (not installable
  CPU-only in this container). Run on your env / CI. See PENDING-GPU.

---

## 4. Posted vs drafted (footer constraint)

Every GitHub comment posted via the tools gets an auto "Generated by Claude
Code" footer I can't suppress or remove (no edit tool, no API token here). Per
your call: **draft mode.** I posted the 5 category-a replies *before* you set
the rule — **please strip their footers** (links in `REVIEW-REPLIES.md → A`).
All other replies (B, 11b, 11c, c2, c3) are drafts there; I post nothing further.
Resolves and code pushes are footer-free and continue normally.

---

## 5. Decisions waiting on you — see DECISIONS-PENDING.md

- **D0** push Phase 2 (`2024b80`) to the live PR? (go/no-go)
- **D1** c1 chunked `generation_steps` — investigated; recommend Approach **B1**
  (`_finalize` subclass skip-set hook). Files/shapes/tests written up.
- **D2** c2 remove gate script / parity harness? (they're the S0 output-layer)
- **D3** c3 rename `MossTTSLocalDecodeJournal`? (in #734/#736 contract)
- **D4** AkazaAkane vectorization — follow-up after this PR + #734.

---

## 6. PENDING-GPU (for you, on a CUDA box — ordered)

1. Install/activate the full env (sglang + runtime) on the GPU host.
2. `pytest tests/unit_test/moss_tts_local/ -q` — full suite incl.
   `test_state_pool.py`, `test_pipeline.py`, `test_radix_hash.py` must be green.
3. **Output-layer (S0 bit-identity) rerun** for the GPU-hash change:
   `python scripts/gate_s0_sync_parity.py` at bs=1/4/16, 50 frames — assert
   bit-identical `audio_codes`/`output_ids` vs pre-`2024b80`. This is the
   behaviour-neutrality proof the 11b reply promises.
4. Sanity: confirm no per-frame D2H in the decode region (e.g. nsys / a
   `torch.cuda.synchronize` count) — `_row_radix_token_ids` should add none.
5. If D0 = go and S0 is green, post 11b on thread 8 and leave it open or resolve
   per your preference.

---

## 7. CI note

`omni-ci-gate` fails in ~5s and skips the GPU stages (Qwen3/TTS CI = "skipped")
— it's a **label/permission gate**, not a code failure. `lint`, `build-docs`,
CodeQL, `test-layout` are green. `mergeable_state: blocked` is from the gate +
unresolved review, not a test break. Needs a maintainer label to run the heavy
stages — not something I can fix in code.

## 8. Watch status

shuaills' review still inbound (API surface → default c, nits → a). No new
comments since the Ratish wave at this writing. Watch rules unchanged.
