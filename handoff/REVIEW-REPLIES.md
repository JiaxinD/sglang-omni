# REVIEW-REPLIES — PR #745

**Posting rule (this session):** I cannot post GitHub comments without an
auto-appended "Generated by Claude Code" footer (no edit/delete tool, no API
token here). Per your call, **everything below is a draft for you to paste**;
I push code + resolve threads (both footer-free) but post no comments.

---

## A. Already posted (category-a) — ACTION: strip the footer on these 5

Posted as JiaxinD, then resolved. Each carries the footer — please edit/delete
(you're the author). Text I intended (footer-free) is shown for reference.

| Thread | Reply link | Intended text |
|---|---|---|
| stages.py:350 | `#discussion_r3399040404` | Fixed in 2f33b06 — trimmed the `abort_request` rationale comment to two lines. |
| state_pool.py:44 | `#discussion_r3399040559` | Fixed in 2f33b06 — condensed the feedback `bf16` comment. |
| test_pipeline.py:650 | `#discussion_r3399040693` | Fixed in 2f33b06 — removed the section banner. |
| state_pool.py:166 | `#discussion_r3399040839` | Fixed in 2f33b06 — replaced internal `PR-B` codename with `#734`. |
| state_pool.py:14 | `#discussion_r3399040979` | Fixed in 2f33b06 — condensed the module/`padding_row` docstring. |

---

## B. gaoyang07 — `reset_for_refill` suggestion (already implemented)

Thread: `#discussion_r3395172184` (model_runner.py). Open + outdated. The
pool-level API he proposed (`reset_for_refill` → `row_for` + `invalidate_params`
+ `reset_row`, no-row → False) already landed in `7ee050b` with two tests
(`test_reset_for_refill_*`). **You can resolve after posting:**

> Implemented in 7ee050b — `MossTTSLocalDecodeStatePool.reset_for_refill(rid)`
> does exactly this (`row_for` → `invalidate_params` + `reset_row`, returns
> `False` when the rid holds no row), and `_build_prefill_input_embeds` calls it
> on the retraction path. Covered by `test_reset_for_refill_clears_active_row` /
> `test_reset_for_refill_is_noop_for_unheld_rid`. Thanks!

---

## 11b. gaoyang07 — GPU-native radix hash (post when Phase 2 lands)

Thread: `#discussion_r3395352785` (model_runner.py:270). **Post only after
`2024b80` is pushed to the live PR (see D0).** Do NOT resolve — GPU rerun
pending.

> Per the dev-group discussion (capture-region D2H constraint raised by
> @Ccyest), we're folding this into this PR: GPU polynomial hash on the
> generated-row path, blake2b kept for prompt preprocessing. Implementation +
> CPU-side verification incoming in this branch; GPU gate rerun to follow.
> Thanks for pushing on this direction.

(Implementation note for you: `2024b80` — `radix_hash.gpu_radix_row_hash`,
delegated from `_row_radix_token_ids`; CPU key-layer tests green; design in
`docs/design/gpu_radix_hash.md`.)

---

## 11c. AkazaAkane — vectorize into [P] pool tensors (follow-up)

Thread: `#discussion_r3398199811` (model_runner.py:173). Draft (do not resolve —
it's a tracked follow-up):

> Fully agree — that's the second half of the pooling work (orchestration into
> [P] tensors, not just state). Tracking it as a follow-up after this PR and
> #734 land; it pairs naturally with the CUDA Graph capture work in #736.

---

## c2. Ratish — `scripts/gate_s0_sync_parity.py` "shouldn't be here"

Threads: `#discussion_r3398085976` (script), `#discussion_r3398238552`
(test_parity_harness.py). Draft (hold — see D2; do not resolve):

> This is the S0 bit-identity gate referenced in the PR's Verification section —
> and now the output-layer check for the generated-row hash change. Rather than
> drop it, I can relocate the standalone script under `tests/` and/or convert
> the gate into a `@pytest.mark.gpu` test so it isn't a stray local script.
> Want me to do that here, or keep it as the documented gate?

## c3. Ratish — rename `MossTTSLocalDecodeJournal`?

Thread: `#discussion_r3398513076` (state_pool.py:159). Draft (hold — see D3; do
not resolve):

> The `…Journal` name is referenced in the #734/#736 interface contract, so I'd
> rather not rename it unilaterally mid-review. `MossTTSLocalDecodeStepOutput`
> reads well if we do change it — I'll align the name with the #734 consumers in
> one pass. Preference?
